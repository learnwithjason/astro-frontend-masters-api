{
	"private": true,
	"name": "astro-frontend-masters-api",
	"version": "1.0.0",
	"description": "To showcase loading data, we’re faking a small REST API to load product data as JSON.",
	"main": "api/products.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Jason Lengstorf <jason@learnwithjason.dev>",
	"license": "MIT",
	"devDependencies": {
		"@netlify/functions": "^1.4.0"
	}
}
